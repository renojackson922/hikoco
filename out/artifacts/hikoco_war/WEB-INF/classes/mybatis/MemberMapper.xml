<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- DAO(Interface) 아래 resultType에는 VO 지정 -->
<mapper namespace="org.silkdog.maven.hikoco.member.dao.MemberDAO">
    <select id="select" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        SELECT * FROM hikoco_member WHERE mem_id = 10
    </select>

    <select id="login" parameterType="java.util.HashMap" resultType="org.silkdog.maven.hikoco.member.vo.MemberVO">
        SELECT * FROM hikoco_member  WHERE mem_userid = #{mem_userid} AND mem_password = #{mem_password};
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="mem_id" parameterType="java.util.HashMap">
        INSERT INTO hikoco_member (mem_userid, mem_password, mem_realname, mem_nickname, mem_photo, mem_tier, mem_phone, mem_birthday, mem_sex, mem_zipcode, mem_addr1, mem_addr2, mem_addr3, mem_addr4, mem_regdate, mem_lastlogin_datetime, mem_lastlogin_ip) VALUES (
        #{mem_userid},
        #{mem_password},
        #{mem_realname},
        #{mem_nickname},
        #{mem_photo},
        #{mem_tier},
        #{mem_phone},
        #{mem_birthday},
        #{mem_sex},
        #{mem_zipcode},
        #{mem_addr1},
        #{mem_addr2},
        #{mem_addr3},
        #{mem_addr4},
        #{mem_regdate},
        #{mem_lastlogin_datetime},
        #{mem_lastlogin_ip}
        )
    </insert>
</mapper>