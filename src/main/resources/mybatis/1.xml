<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- DAO(Interface) 아래 resultType에는 DTO 지정 -->
<mapper namespace="org.silkdog.maven.hikoco.member.dao.MemberDAO">
    <select id="select" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        select * from HIKOCO_MEMBER where MEM_ID = 10
    </select>

    <!--<insert id="insert" parameterType="java.util.HashMap">-->
      <!--<selectKey keyProperty="MEM_ID" resultType="int" order="BEFORE">-->
        <!--select HIKOCO_MEMBER_SEQ.nextval from DUAL-->
      <!--</selectKey>-->
        <!--insert into HIKOCO_MEMBER values()-->
    <!--</insert>-->



    <insert id="insert" useGeneratedKeys="true" keyProperty="mem_id" parameterType="org.silkdog.maven.hikoco.member.dto.MemberDTO">
        INSERT INTO HIKOCO_MEMBER (mem_userid, mem_password, mem_realname, mem_nickname, mem_photo, mem_tier, mem_phone, mem_birthday, mem_sex, mem_zipcode, mem_addr1, mem_addr2, mem_addr3, mem_addr4, mem_regdate, mem_lastlogin_datetime, mem_lastlogin_ip) VALUES (
        #{mem_userid},
        #{mem_password},
        #{mem_realname},
        #{mem_nickname},
        #{mem_photo},
        #{mem_tier},
        #{mem_phone},
        #{mem_birthday},
        #{mem_sex},
        #{mem_zipcode},
        #{mem_addr1},
        #{mem_addr2},
        #{mem_addr3},
        #{mem_addr4},
        #{mem_regdate},
        #{mem_lastlogin_datetime},
        #{mem_lastlogin_ip}
        )
    </insert>
</mapper>